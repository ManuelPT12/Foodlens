// lib/screens/dietist_page.dart
import 'package:flutter/material.dart';

class DietistPage extends StatelessWidget {
  const DietistPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: reemplaza con tu contenido real de “Dietista”
    return Center(
      child: Text(
        'Contenido de “Dietista”',
        style: TextStyle(fontSize: 18, color: Color(0xFF0F3C33)),
      ),
    );
  }
}
