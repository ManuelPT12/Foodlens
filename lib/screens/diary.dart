// lib/screens/diary_page.dart
import 'package:flutter/material.dart';

class DiaryPage extends StatelessWidget {
  const DiaryPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: reemplaza con tu contenido real de “Diario”
    return Center(
      child: Text(
        'Contenido de “Diario”',
        style: TextStyle(fontSize: 18, color: Color(0xFF0F3C33)),
      ),
    );
  }
}
