// lib/screens/what_to_eat_page.dart
import 'package:flutter/material.dart';

class WhatToEatPage extends StatelessWidget {
  const WhatToEatPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: reemplaza con tu contenido real de “Qué comer”
    return Center(
      child: Text(
        'Contenido de “Qué comer”',
        style: TextStyle(fontSize: 18, color: Color(0xFF0F3C33)),
      ),
    );
  }
}
